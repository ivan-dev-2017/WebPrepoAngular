import { AppExampleZippyDirective } from './app-example-zippy.directive';

describe('AppExampleZippyDirective', () => {
  it('should create an instance', () => {
    const directive = new AppExampleZippyDirective();
    expect(directive).toBeTruthy();
  });
});
